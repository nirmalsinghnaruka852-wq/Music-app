import ThemeText from "@/Shared/Stores/Theme/Components/ThemeText";
import ThemeView from "@/Shared/Stores/Theme/Components/ThemeView";

export default function PlayerScreen() {
    return (
        <ThemeView>
            <ThemeText>PlayerScreen</ThemeText>
        </ThemeView>
    )
}